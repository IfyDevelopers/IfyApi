import { stopBot, startBot, restartBot, shutdown } from './setup.js';

export type { platformList } from './setup.js';

export {
    stopBot,
    startBot,
    restartBot,
    shutdown
};

export default {
    stopBot,
    startBot,
    restartBot,
    shutdown
};